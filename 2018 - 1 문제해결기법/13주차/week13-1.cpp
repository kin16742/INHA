/*#include <iostream>
#include <algorithm>
#include <string.h>
using namespace std;

int n, c, d, a[100000], b[100000], length;

int main() {
	std::ios::sync_with_stdio(false);
	cin.tie(NULL);

	cin >> n >> c >> d;
	for (int i = 0; i < n; i++) {
		cin >> a[i] >> b[i];
	}
	abs(b - a) + abs(c - d);
	cout << length << '\n';
	return 0;
}*/